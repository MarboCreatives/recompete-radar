# Vendors that must NEVER be treated as individual people.
#
# is_individual() is deliberately biased toward withholding: a false positive
# costs a reader one company name, a missed suppression exposes a private
# person. This file is how a real organisation gets its name back when a rule
# reads it as a person.
#
# One name per line, exactly as it appears in vendor_name. Matching is on the
# normalised form, so case and punctuation do not matter. A '#' starts a
# comment. A missing file is fine — it just means nothing is allowlisted.
#
# Add a name here only after checking it really is an organisation. Every entry
# is a deliberate decision to publish a name the rules wanted to withhold.

# Accounting firm. Trips is_individual rule 3 because "raymond" is a given name
# and none of "chabot", "grant" or "thornton" is a corporate word. Verified
# against the live vendor list as the only false positive that rule produces.
RAYMOND CHABOT GRANT THORNTON

# Added 14 September 2026 with the individual-name fixes. Each is a company the
# widened given-name test now reads as a person. Measured, not guessed: these
# three were the only unambiguous firms among the 172 names the change newly
# withheld. Names that could be a person trading under their own name were left
# withheld, because withholding a company costs a reader one label and missing a
# person exposes them.
STEWART MCKELVEY
RENE BLAIS LTE
rene blaise lite

# Added 21 September 2026 with is_individual rule 4, the dotted-initials test.
#
# Rule 4 withholds dotted initials in front of a surname, because an
# initialised given name can never be in GIVEN_NAMES and so rules 2 and 3
# could never reach it. That is how a real person was published and found on
# 21 September.
#
# The rule cannot tell a person from a firm using the same form. Measured on
# the live vendor list, rule 4 and the two given names added in the same
# change withhold 10 names, of which 7 are people. These three are the firms.
# Each was checked by hand against its contracts, value, postal prefix and
# department before being listed here.
#
# The wider version of the rule was measured too and rejected: it withholds
# 58 names of which 44 are companies, and audit.py refuses it by name.
G.C. CARPENTRY
R.M AUTOTECH
S.S CONSTRUCTIONS
